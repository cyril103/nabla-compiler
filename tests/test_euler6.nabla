import collections.int_array

def main(): Int = {
    val values = arrayIntRangeUntil(1, 101)
    val sumOfSquares = values
        .map(value => value * value)
        .sum()
    val sum = values.sum()
    val squareOfSum = sum * sum

    print((squareOfSum - sumOfSquares).toString())
    0
}
