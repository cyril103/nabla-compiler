import collections.array

def main(): Int = {
    val xs = Array.fill[Bool](3, true)
    xs.set(1, false)
    val inverted = xs.map(value => !value)
    if xs.countTrue() == 2 && xs.any() && !xs.all() && inverted.countTrue() == 1 {
        42
    } else {
        1
    }
}
