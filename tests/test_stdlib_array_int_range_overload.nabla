import collections.array

def main(): Int = {
    val values = Array.range(2, 5)
    val empty = Array.range(5, 2)
    if values.size() == 3 && values.get(0) == 2 && values.get(2) == 4 && empty.size() == 0 {
        42
    } else {
        1
    }
}
