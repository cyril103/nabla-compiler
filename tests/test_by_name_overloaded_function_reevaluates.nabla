def use(x: => Int): Int = {
    x + x
}

def use(x: String): Int = {
    0
}

class Counter(var value: Int) {
    def next(): Int = {
        value = value + 1
        value
    }
}

def main(): Int = {
    val counter = new Counter(0)
    use(counter.next())
}
