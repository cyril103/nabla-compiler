import collections.array

def main(): Int = {
    val xs = Array.range(3)
    val appended = xs.append(9)
    val prepended = xs.prepend(7)
    appended.size() + appended.last() + prepended.head() + prepended.get(1)
}
