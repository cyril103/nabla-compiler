class Pair(val left: Int, val right: Int) {
    def sum(): Int = {
        left + right
    }
}

def main(): Int = {
    match new Pair(1, 2) {
        Pair(value, value) => value
        _ => 0
    }
}
