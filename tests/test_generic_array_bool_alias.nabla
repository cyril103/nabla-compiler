import collections.bool_array

def count(values: Array[Bool]): Int = {
    values.countTrue()
}

def main(): Int = {
    if count(arrayBool(boolArrayFill(4, true))) == 4 {
        42
    } else {
        1
    }
}
