import collections.array

def main(): Int = {
    val values = Array.rangeUntil(2, 5)
    val empty = Array.rangeUntil(5, 2)

    if values.mkString(",") == "2,3,4" &&
       values.sum() == 9 &&
       empty.size() == 0 {
        42
    } else {
        1
    }
}
