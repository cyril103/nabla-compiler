import collections.int_array

def main(): Int = {
    val xs = arrayIntRange(5).append(3)
    if xs.indexOfOption(3).isEmpty() {
        99
    } else {
        xs.indexOfOption(3).getOrElse(0)
    }
}
