class Base(name: String, age: Int) {
    def description(): String = {
        name
    }
}

class Student extends Base(name, age)(score: Int) {
    def score(): Int = {
        score
    }
}


def main(): Int = {
    (new Student("Ada", 19, 100)).score()
}
