import collections.array

def main(): Int = {
    val xs = Array.range(0)
    if xs.min().isEmpty() && xs.max().isEmpty() {
        xs.min().getOrElse(20) + xs.max().getOrElse(22)
    } else {
        1
    }
}
