import collections.list

def main(): Int = {
    val asAny: Any = Nil
    val asRef: AnyRef = Nil
    if Nil.isEmpty() && !Nil.nonEmpty() && Nil.headOption().isEmpty() && Nil.mkString(",") == "" && asAny.equals(asRef) {
        42
    } else {
        1
    }
}
