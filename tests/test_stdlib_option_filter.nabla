import core.option

def main(): Int = {
    val kept = Option.some[Int](42).filter(value => value > 10)
    val dropped = Option.some[Int](7).filter(value => value > 10)
    kept.getOrElse(0) + dropped.getOrElse(0)
}
