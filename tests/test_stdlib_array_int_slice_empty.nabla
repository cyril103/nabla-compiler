import collections.int_array

def main(): Int = {
    val xs = arrayIntRange(5)
    val reversedBounds = xs.slice(4, 2)
    val equalBounds = xs.slice(3, 3)
    if reversedBounds.isEmpty() && equalBounds.isEmpty() {
        42
    } else {
        1
    }
}
