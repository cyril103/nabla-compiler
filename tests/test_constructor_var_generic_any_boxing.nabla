class Holder(var value: Any) {
    def replace[T](next: T): Int = {
        value = next
        if value.toString().length() == 2 { 42 } else { 1 }
    }
}

def main(): Int = {
    new Holder("start").replace[Int](42)
}
