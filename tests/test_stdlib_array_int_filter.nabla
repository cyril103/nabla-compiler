import collections.int_array

def main(): Int = {
    val xs = new ArrayInt(new IntArray(5))
    xs.raw().set(0, 1)
    xs.raw().set(1, 0)
    xs.raw().set(2, 3)
    xs.raw().set(3, 0)
    xs.raw().set(4, 5)
    xs.filter((value: Int) => {
        value > 0
    }).sum()
}
