import collections.array

def main(): Int = {
    val xs = Array.range(3)
    val taken = xs.take(0 - 2)
    val dropped = xs.drop(0 - 2)
    if taken.isEmpty() && dropped.size() == 3 && dropped.head() == 0 {
        42
    } else {
        1
    }
}
