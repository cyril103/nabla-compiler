import collections.int_array

def main(): Int = {
    val xs = arrayIntRange(5)
    val left = xs.slice(0 - 2, 2)
    val right = xs.slice(3, 99)
    left.size() + left.last() + right.size() + right.head() + right.last()
}
