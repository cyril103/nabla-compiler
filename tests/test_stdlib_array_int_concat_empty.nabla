import collections.array

def main(): Int = {
    val empty = Array.range(0)
    val values = Array.range(3)
    val leftEmpty = empty.concat(values)
    val rightEmpty = values.concat(empty)
    leftEmpty.size() + leftEmpty.last() + rightEmpty.size() + rightEmpty.last()
}
