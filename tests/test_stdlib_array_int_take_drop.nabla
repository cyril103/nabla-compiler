import collections.int_array

def main(): Int = {
    val xs = arrayIntRange(5)
    val taken = xs.take(3)
    val dropped = xs.drop(2)
    taken.size() + taken.last() + dropped.size() + dropped.head()
}
