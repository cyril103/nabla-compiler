import collections.array

def main(): Int = {
    val empty = Array.range(0)
    val appended = empty.append(4)
    val prepended = empty.prepend(5)
    appended.size() + appended.head() + prepended.size() + prepended.last()
}
