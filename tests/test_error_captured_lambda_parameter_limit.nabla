def apply6(f: (Int, Int, Int, Int, Int, Int) => Int): Int = {
    f(1, 2, 3, 4, 5, 6)
}

def main(): Int = {
    val offset = 1
    apply6((a: Int, b: Int, c: Int, d: Int, e: Int, f: Int) => {
        a + b + c + d + e + f + offset
    })
}
