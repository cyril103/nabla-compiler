import collections.int_array

def main(): Int = {
    val xs = arrayIntRange(3)
    val taken = xs.take(9)
    val dropped = xs.drop(9)
    if taken.size() == 3 && taken.last() == 2 && dropped.isEmpty() {
        42
    } else {
        1
    }
}
