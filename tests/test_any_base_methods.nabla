class Plain() {
}

class Person() {
    override def toString(): String = {
        "person"
    }

    override def hashCode(): Int = {
        99
    }
}

def main(): Int = {
    val plain = new Plain()
    val identity = plain.toString() == plain.toString() && plain.hashCode() == plain.hashCode()
    val overrideValue = new Person()
    if overrideValue.toString() == "person" && overrideValue.hashCode() == 99 {
        if identity {
            0
        } else {
            1
        }
    } else {
        1
    }
}
