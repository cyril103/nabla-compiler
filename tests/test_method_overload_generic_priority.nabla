class Box() {
    def pick(value: Int): Int = {
        value + 1
    }

    def pick[T](value: T): Int = {
        10
    }
}

def main(): Int = {
    val box = new Box()
    box.pick(41) + box.pick("generic") - 10
}
