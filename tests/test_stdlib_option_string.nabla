import core.option

def main(): Int = {
    val some = Option.some[String]("hello")
    val none = Option.none[String]("fallback")
    some.getOrElse("x").length() + none.getOrElse("fallback").length() + 29
}
