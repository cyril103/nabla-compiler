import collections.list

def main(): Int = {
    val builder = new ListBuilder[Int](List.empty[Int]())
    builder.add(1)
    builder.add(2)
    builder.add(3)
    val values = builder.result()
    values.head() * 100 + values.tail().head() * 10 + values.tail().tail().head()
}
