import core.option

def main(): Int = {
    val some = Option.some[Int](41)
    val none = Option.none[Int]()
    val mappedSome = some.map[Int](0, value => value + 1)
    val mappedNone = none.map[Int](42, value => value + 1)
    mappedSome.getOrElse(0) + mappedNone.getOrElse(0)
}
