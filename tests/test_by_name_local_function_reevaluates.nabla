class Counter(var value: Int) {
    def next(): Int = {
        value = value + 1
        value
    }
}

def main(): Int = {
    val counter = new Counter(0)
    def twiceLocal(x: => Int): Int = {
        x + x
    }
    twiceLocal(counter.next())
}
