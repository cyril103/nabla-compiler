def main(): Int = {
    val text = "yes"
    val first = text.charAt(0)
    val second = text.charAt(1)

    if first == 'y' &&
       second != 'y' &&
       '\n' != first {
        42
    } else {
        1
    }
}
