import collections.array

def sameArray[T](xs: Array[T]): Array[T] = {
    xs
}

def chooseArray[T](left: Array[T], right: Array[T], useLeft: Bool): Array[T] = {
    if useLeft {
        left
    } else {
        right
    }
}

def main(): Int = {
    val ints = sameArray(Array.fill[Int](3, 14))
    val longs = chooseArray(Array.fill[Long](1, 1L), Array.fill[Long](2, 21L), false)
    val bools = sameArray(arrayMap[Bool](Array.fill[Bool](2, false), value => !value))
    if ints.sum() == 42 && longs.sum() == 42L && bools.countTrue() == 2 {
        42
    } else {
        1
    }
}
