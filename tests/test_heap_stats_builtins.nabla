def main(): Int = {
    val before = heapUsed()
    val capacity = heapCapacity()
    val xs = new IntArray(3)
    xs.set(0, 1)
    val after = heapUsed()
    if capacity > 0 && before >= 0 && after > before && xs.get(0) == 1 {
        0
    } else {
        1
    }
}
