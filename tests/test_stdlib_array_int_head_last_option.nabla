import collections.array

def main(): Int = {
    val xs = Array.range(3)
    val empty = Array.range(0)
    xs.headOption().getOrElse(10) + xs.lastOption().getOrElse(20) +
        empty.headOption().getOrElse(30) + empty.lastOption().getOrElse(40)
}
