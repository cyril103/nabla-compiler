import core.option

def apply(value: Option[Int], transform: (Option[Int]) => Int): Int = {
    transform(value)
}

def main(): Int = {
    apply(Option.some(42), (value: Option[Int]) => {
        value.getOrElse(0)
    })
}
