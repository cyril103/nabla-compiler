import core.option_int

def apply(value: Option[Int], transform: (Option[Int]) => Int): Int = {
    transform(value)
}

def main(): Int = {
    apply(optionIntSome(42), (value: Option[Int]) => {
        value.getOrElse(0)
    })
}
