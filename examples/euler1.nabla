def main(): Int = {
    var total = 0
    var value = 1
    while value < 1000 {
        if value % 3 == 0 || value % 5 == 0 {
            total = total + value
        } else {
            total = total
        }
        value = value + 1
    }
    print(total.toString())
    0
}
